<%= turbo_frame_tag id="content-slider", target: '_top', class:"w-full", data: {turbo_action: "advance"} do%>
  <article id="animation-region" class="flex flex-col w-full animate__animated animate__fadeInRight">
    <h1 id="hold"class="text-neutral font-bold text-4xl font-josefin text-center pb-1">List of my blogs:</h1>
    <%= turbo_frame_tag id="project-information", target: '_top', class:"w-full" do%>
    <% end %>
    <div id="project-grid" class="grid grid-cols-1 gap-x-1 gap-y-3 lg:grid-cols-2 xl:grid-cols-3 mt-2 mx-auto">
      <%= render @blogs %>
    </div>
  </article>
<% end %>
